services:
  bot:
    build: .
    env_file: .env
    restart: unless-stopped
    environment:
      - DB_PATH=/app/data/data.db
    volumes:
      - bot_data:/app/data
      - ./creds.json:/app/creds.json

volumes:
  bot_data:



